---
label: Section
hide_body: true
is_partial: true
display_field: type
fields:
  - type: text
    name: section_id
    label: Section ID
    config:
      required: true
  - type: text
    name: identifier
    label: Identifier
    description: Used by the CMS to identify the section (not used by the page)
    config:
      required: true
  - type: text
    name: component
    hidden: true
    config:
      required: true
